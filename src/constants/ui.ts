/** Presentation-only constants. Kept here so no magic numbers appear in components. */

/** Decimal places used when displaying kg CO2e values. */
export const DISPLAY_DECIMALS = 1;

/** Multiplier to turn a ratio into a percentage. */
export const PERCENT_MULTIPLIER = 100;

/** Height of the trend chart in pixels. */
export const CHART_HEIGHT = 320;

/** Step for numeric activity inputs. */
export const NUMBER_INPUT_STEP = 0.1;

/** Maximum length of the optional free-text note on an entry. */
export const NOTE_MAX_LENGTH = 120;
